## Dvd Screen Saver
Probably the stupidest project you will ever see, a Dvd screen saver written in python using pygame. This project is pretty simple as far as coding goes, and has options for the user to do whatever with. There are a couple bugs to work out but those won't take long.

## Dowloads
Just go to the releases tab and download the [lastest version]() directly from there.

<!-- Contributing -->
## Contributing
If you want a feature, feel free to add it!

1. Fork the repository
2. Create your branch (`git checkout -b feature`)
3. Commit changes (`git commit -m 'add my python'`)
4. Push to your branch (`git push origin feature`)
5. Open a pull request

## Prerequisites
You'll need python 3.12 installed, as pygame doesn't support the newest versions of python. I'm using [3.12.4](https://www.python.org/downloads/release/python-3124/), as it was the latest version when pygame was last updated. I'd imagine that any 3.12 version works though. You'll also probably want an IDE like Visual Studio Code. You could raw dog it with the python IDE but why, why would you do that?

## Setup
1. Clone your fork
```sh
git clone '<Your fork URL here>'
cd Dvd-Screen-Saver-Py
```
2. Install the packages
```sh
pip install -r requirements.txt
```